# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CounterReSwiftSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CounterReSwiftSample
    source 'https://github.com/CocoaPods/Specs.git'
    platform :ios, '8.0'

    pod 'ReSwift'

  target 'CounterReSwiftSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CounterReSwiftSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
